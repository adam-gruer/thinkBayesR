Dict <- R6::R6Class("Dict",
                    public = list(
                      initialize = function(values = numeric(), probs = numeric){
                        l = length(values)
                        if (any)
                        
                        
                        
                      }
                    ),
                     private = list(
                      dict = NULL,
                      
                      ,
                    )
                    )